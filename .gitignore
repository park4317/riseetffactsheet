# ── 자격증명 — 절대 커밋 금지 ────────────────────────────────
# 2026-09-10 감사에서 pipeline/config.py 의 평문 DART/KRX 키가
# 이 저장소의 공개 히스토리에 들어 있는 것이 확인됐다.
# 키는 .env 로만 주입한다. 이 줄을 지우지 말 것.
.env
.env.*
!.env.example
*.key
secrets.*

# ── 파이썬 ──────────────────────────────────────────────────
__pycache__/
*.pyc
.venv/
venv/

# ── 산출물 캐시 ─────────────────────────────────────────────
*.log
